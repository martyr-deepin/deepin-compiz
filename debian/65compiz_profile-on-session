# This file is sourced by Xsession(5), not executed.
# Select deepin profile if we are in the default session.
# The default profile is used otherwise.

if [ "x$DESKTOP_SESSION" = "xneko" ]; then
    COMPIZ_CONFIG_PROFILE="deepin"
    export COMPIZ_CONFIG_PROFILE
fi

